/*import React from "react";
import { render, screen } from "@testing-library/react";

import App from "./App";

describe("", () => {
  describe("", () => {
    it("", () => {
    
      render(<App />);

      expect(screen.getByText("")).toBeInTheDocument();// test or expect
    });
  });
});*/
